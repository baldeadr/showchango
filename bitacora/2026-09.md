# Bitácora — 2026-09

> Registro de avances y decisiones de Show Chango. Formato definido en `AGENTS.md`.

### 2026-09-18 — F0: nace el repo de Show Chango

**Qué hice:**

- Creé el repo independiente `~/Projects/showchango` con `git init`, `.gitignore`, `pyproject.toml` y `Dockerfile` base.
- Escribí la documentación base: `README.md`, `AGENTS.md`, `docs/ARQUITECTURA.md`, `docs/FASES.md` y `docs/ESQUEMA-JSON.md`.
- Implementé el esquema JSON v1 en `src/showchango/core/esquema.py` con validaciones y round-trip.
- Levanté el esqueleto FastAPI: home, crear set, set vacío en sesión temporal RAM y exportar JSON.
- Verifiqué que `pytest`, `ruff check` y `uvicorn` funcionen.

**Qué aprendí:**

- Al decidir que **nada se almacena** (sets temporales, JSON como persistencia), la arquitectura se simplifica drásticamente: no hay cuentas, no hay tokens de enlace, el deploy en free tier no requiere disco persistente y la privacidad se convierte en diferenciador de producto.
- El esquema JSON v1 debe definirse primero y congelarse: es el contrato del round-trip y la base para que cualquier motor de análisis (librosa, Essentia, Essentia.js) pueda alimentar la misma estructura de setlist.

**Decisión tomada:**

- Show Chango tiene su propio repo (`~/Projects/showchango`) y su propia bitácora; la ficha de `architecting-a-band/show-chango/README.md` pasa a ser archivo histórico.
- Stack confirmado: FastAPI + HTMX + SortableJS + Chart.js + librosa + ThreadPool + WeasyPrint + matplotlib + SQLite solo como caché de análisis.
- Spotify/metadata fuera del MVP; entra en F2.
- Nada del usuario se persiste: sesión temporal + JSON exportado como única persistencia real.

**Siguiente paso:**

- F1-M1: importar pistas (formulario manual + CSV/Excel + subida de audio con análisis efímero y caché por hash) e implementar export/import JSON como mecanismo de guardado desde el día 1.

### 2026-09-18 — Logo de Show Chango en SVG

**Qué hice:**

- Creé `src/showchango/web/static/logo.svg` (512×512) a partir del prompt de imagen de Gemini: cara de chango de circo con sombrero de copa, cortinas rojas laterales y valance festoneado, spotlight con acento neón `#5B8CFF`, fondo `#0B0B0C`, sin texto ni gradientes.
- Lo integré en la web: favicon y logo en el header (`base.html`), estilos `.marca` en `estilo.css`.
- Agregué `test_logo_svg` en `tests/test_web.py` (sirve el SVG y el home lo referencia). `pytest` y `ruff` en verde.

**Qué aprendí:**

- En `url_for` de Jinja las rutas salen absolutas (`http://testserver/static/logo.svg`), por eso el test verifica `/static/logo.svg` como subcadena.
- No puedo ver imágenes directamente: verifiqué el render con análisis de píxeles por Python y con un grid de 16×16, donde el logo se lee como chango con sombrero enmarcado por cortinas rojas.

**Decisión tomada:**

- Logo en versión única oscura (según el prompt); el chango en caramelo con muzzle crema.

**Siguiente paso:**

- F1-M1: importar pistas e implementar export/import JSON como mecanismo de guardado.

### 2026-09-18 — F1-M1: importar pistas (manual, CSV/Excel, audio, JSON)

**Qué hice:**

- Agregué `librosa`, `openpyxl` y `soundfile` al stack; `Dockerfile` ahora instala `ffmpeg`.
- Creé el módulo `analytics` con la interfaz `Analyzer` y su implementación `LibrosaAnalyzer` (BPM, duración, energía, loudness, bailabilidad heurística, valencia por modo mayor/menor, tonalidad por Krumhansl-Kessler).
- Creé la caché de análisis por hash en SQLite (`showchango/db/__init__.py`) para no repetir análisis.
- Implementé en la web: formulario manual de pista, edición de atributos, eliminación, importar CSV/Excel, importar JSON (round-trip), y subida de audio con análisis en background + HTMX polling.
- Actualicé las plantillas (`set.html`, `index.html`, `analisis_estado.html`) y los estilos.
- Agregué tests para todas las vías de importación, incluyendo audio sintético a 120 BPM. Total: 23 tests, `ruff check` limpio.

**Qué aprendí:**

- librosa 1.0 cambió la API: `librosa.feature.rhythm.tempo` reemplaza al antiguo `librosa.beat.tempo`; hay que importar el submódulo `rhythm` explícitamente.
- TestClient de FastAPI ejecuta las `BackgroundTasks` antes de devolver el response, lo que simplifica los tests de análisis asíncrono.
- Mapear encabezados de CSV/Excel con y sin tildes hace la importación usable con hojas reales del artista.

**Decisión tomada:**

- Los atributos de librosa son un punto de partida editable; la UI deja cambiar cualquier campo. La caché solo guarda atributos anónimos por hash, nunca audio.
- El análisis de voz/instrumental queda como `[PENDIENTE]` para un modelo posterior; por ahora `voz` es `None` y el usuario lo indica a mano.

**Siguiente paso:**

- F1-M2: curva + drag & drop (SortableJS + Chart.js).

### 2026-09-18 — F1-M2: curva + drag & drop

**Qué hice:**

- Creé el módulo puro `showchango/core/curva.py` para calcular la curva de energía/baile a partir del orden del set (tiempo acumulado, energía, bailabilidad, duración).
- Agreguí endpoints `/set/curva-json` y `/set/reordenar`: el cliente envía el nuevo orden vía JSON, el servidor actualiza la sesión y devuelve los datos actualizados de la curva.
- Integré SortableJS y Chart.js vía CDN; creé `static/js/set.js` para inicializar la lista arrastrable y el gráfico, y recalcular la curva en vivo al soltar una pista.
- Rediseñé `set.html`: curva arriba, lista arrastrable con handle, tabla de edición debajo.
- Agregué tests para la curva y el reordenamiento. Total: 31 tests, `ruff check` limpio.

**Qué aprendí:**

- Separar el cálculo de la curva en `core` (puro) y dejar la UI solo para renderizado y eventos respeta SOLID: el dominio no depende de Chart.js ni de SortableJS.
- `stepped: 'after'` en Chart.js es la forma más clara de representar una curva por bloques (cada pista mantiene su energía durante su duración).

**Decisión tomada:**

- La curva se recalcula en el cliente tras un POST al servidor: el orden se persiste en la sesión temporal y la UI actualiza Chart.js con la respuesta. Es simple, robusto y no requiere WebSockets.

**Corrección inmediata 1:** cambié todos los redirects post-form de 302 a 303 para evitar que el navegador repita el POST en el destino (`/set` devolvía 405 y parecía que "no hacía nada").

**Corrección inmediata 2:** `_float_or_none` ahora acepta duración en formato `m:ss` (p. ej. `3:10` → 190 s) y muestra un mensaje de error en la página en lugar de un `Internal Server Error`. También cubrí esto con tests.

**Siguiente paso:**

- F1-M3: diagnóstico (plantillas de arco, detección de valles/picos y alertas de bloques repetidos).

### 2026-09-18 — Logo recreado en SVG a partir del JPEG de referencia

**Qué hice:**

- El artista aportó `logo.jpeg` (1024×1024, generado en Gemini) como referencia. Un cleanup posterior lo eliminó del repo (`a2209e4`); lo recuperé desde el historial de git (`git show 9d8b437:...`) para analizarlo.
- Recreé `src/showchango/web/static/logo.svg` vectorizando esa referencia: cortinas rojas `#B21234` con sombra `#8C0D2B`, fondo `#090A0C`, haz de luz azul `#6E86E6` con núcleo `#CFE9FF`, chango caramelo `#CD833A` con orejas interiores oscuras `#A35E25`, cara crema `#F8F1D7` (peanut: dos elipses), ojos/nariz/boca oscuros, y sombrero negro con banda roja y contorno neón.
- Verifiqué imposibilidad de ver imágenes: comparé la geometría y la paleta con clasificación de color por píxel (grids 16×16 y 56×56) contra el JPEG, iterando hasta que la silueta y los colores coincidieran.
- `pytest` (33) y `ruff check` en verde.

**Qué aprendí:**

- Como no veo imágenes, el flujo confiable es extraer la paleta cuantizada y hacer máscaras por color (corridas de píxeles por fila) para reconstruir posiciones y tamaños exactos.
- El sombrero es negro sobre fondo negro: solo se distingue por el trazo neón; por eso el `.svg` debe dibujar el contorno azul aunque el relleno sea casi idéntico al fondo.

**Decisión tomada:**

- El logo oficial es el SVG vectorial recreado del JPEG (nítido a 16×16); el JPEG no se conserva en `static/` para no cargar 422 KB innecesarios. Queda recuperable en el historial de git.

**Corrección:** las orejas quedaban como blobs separados de la cabeza (dos elipses que apenas se tocaban). Las agrandé, las incliné hacia la cabeza y las dibujo antes que el círculo craneal, que las recorta: así se funden en una sola silueta, como en la referencia.

**Ajuste iOS:** agregué brillos blancos tipo icono de iOS — gloss en frente y orejas, brillo en copa/ala del sombrero, sheen superior y un punto especular en cada ojo. Todo vectorial con `clip-path` y opacidades bajas, sin gradientes. Se mantiene legible a 16×16.

**Ajuste sombrero:** el sombrero estaba mal dibujado: la copa se abría demasiado y la banda roja quedaba parcialmente detrás del ala (dejaba una franja negra entre la banda y el ala). Rediseñé la copa con un flare moderado, la banda sigue el ancho real de la copa y el ala es una elipse alineada con su base.

**Siguiente paso:**

- F1-M3: diagnóstico (plantillas de arco, detección de valles/picos y alertas de bloques repetidos).

### 2026-09-18 — F1-M3: diagnóstico del set

**Qué hice:**

- Creé el módulo puro `showchango/core/diagnostico.py` con modelos `Alerta`, `ArcoPlantilla` y `Diagnostico`.
- Implementé tres plantillas de arco: `climax_70`, `construccion_dj` y `picos_rock`.
- Implementé detectores: valles/picos, bloques repetidos (≥3 pistas con BPM/energía similares) y posiciones desperdiciadas (pico temprano, valle final, pistas sin duración).
- Integré el diagnóstico en `set.html`: selector de plantilla y lista de alertas clasificadas por tipo.
- Agregué tests para cada detector y plantilla. Total: 42 tests, `ruff check` limpio.

**Qué aprendí:**

- Los diagnósticos deben quedar en `core` y ser puros: reciben un `Proyecto` y devuelven datos, sin tocar UI ni sesión.
- Las plantillas de arco son heurísticas configurables; el artista puede cambiar de plantilla y ver en tiempo real cómo cambian las alertas.

**Decisión tomada:**

- Las alertas son sugerencias, no bloqueos: el artista sigue teniendo control total del orden y atributos.

**Siguiente paso:**

- F1-M4: export completo (CSV, Markdown, PNG/SVG, PDF) y test de oro exportar → importar → exportar.

### 2026-09-18 — F1-M4: export completo + test de oro

**Qué hice:**

- Creé la capa `showchango/render/` con exportadores puros:
  - `csv_exporter.py`: pistas en orden del set con duración en segundos y `mm:ss`.
  - `markdown_exporter.py`: setlist en tabla, metadata y curva de energía legible por LLMs.
  - `chart_exporter.py`: PNG/SVG de la curva con matplotlib (backend `Agg`).
  - `pdf_exporter.py`: PDF con setlist y curva embebida vía WeasyPrint.
- Agregué endpoints `/set/exportar-csv`, `/set/exportar-md`, `/set/exportar-png`, `/set/exportar-svg` y `/set/exportar-pdf`.
- Actualicé la sección de acciones de `set.html` con botones para cada formato.
- Agregué `matplotlib` y `weasyprint` a `pyproject.toml` como dependencias principales.
- Escribí `tests/test_render.py` con tests para cada exportador y el test de oro: exportar JSON → importar JSON → exportar JSON = idéntico.
- Total: 48 tests, `ruff check` limpio.

**Qué aprendí:**

- Separar los exportadores en una capa `render` (no `core`) mantiene el núcleo puro mientras se acoplan librerías de presentación como matplotlib y WeasyPrint.
- El test de oro asegura que el esquema JSON v1 realmente preserva todo el estado del set: exportar, importar y volver a exportar debe dar el mismo bytes.

**Decisión tomada:**

- Los exportadores viven en `showchango/render/`; cada uno es una función que recibe `Proyecto` y devuelve bytes/str. La web solo orquesta headers y respuestas.

**Siguiente paso:**

- F2: landing pública, deploy en free tier y evaluar búsqueda/metadata con Spotify.

### 2026-09-18 — F2: landing, deploy y Spotify básico

**Qué hice:**

- Rediseñé `index.html` como landing page: hero, features, privacidad por diseño y CTA a crear set.
- Creé `/privacidad` con explicación clara de cookies, sesión temporal, audio descartado y caché anónimo.
- Mejoré `Dockerfile`: usuario no-root, dependencias de WeasyPrint (Pango/Cairo) y fuentes Liberation.
- Escribí `docs/DEPLOY.md` con instrucciones para Docker, Fly.io y Render.
- Implementé `showchango/spotify.py` con búsqueda de metadata básica (título, artista, duración, imagen) vía Client Credentials; dejé claro que audio features está deprecado por Spotify.
- Agregué sección de búsqueda Spotify en `set.html` con HTMX y partial `spotify_resultados.html`.
- Agregué `httpx` a dependencias principales.
- Actualicé `README.md`, `docs/FASES.md` y creé `docs/SPOTIFY.md`.
- Total: 49 tests, `ruff check` limpio.

**Qué aprendí:**

- Spotify deprecó `/audio-features` en noviembre de 2024, así que la integración solo puede traer metadata básica; energía/BPM deben venir del análisis local o edición manual.
- Una landing con mensaje de privacidad claro refuerza el diferenciador del producto: nada se guarda.

**Decisión tomada:**

- La búsqueda Spotify es opcional y se activa con variables de entorno; sin credenciales la app sigue funcionando.

**Pendientes por decisión del artista:**

- Modelo de acceso (gratis, freemium, pago).
- Si mantener el modelo sin almacenamiento como feature permanente.
- Licencia del proyecto, validación de marca/dominio/handles.

### 2026-09-18 — Decisiones de F2

**Decidido:**

1. **Modelo de acceso:** gratis con full access. Si en el futuro se monetiza, podría explorarse donaciones o features opcionales, pero la base seguirá libre.
2. **Sin almacenamiento:** se mantiene como feature permanente por ahora. El almacenamiento en la nube queda como posible feature paga solo si se monetiza.
3. **Licencia:** omitido por el momento.
4. **Marca/dominio/handles:** omitido por el momento; es un proyecto personal y se renombra si surge un conflicto.

**Siguiente paso:**

- Terminar de pulir F2 (README y docs actualizados) y pasar a F3 solo si hay interés, o cerrar esta sesión.

### 2026-09-18 — Pulido de F2: deploy, salud, SEO y errores amigables

**Qué hice:**

- Agregué `.dockerignore`, `fly.toml` y `render.yaml` para deploy real en free tier.
- Mejoré `Dockerfile` para producción (aunque no pude probar el build local porque Docker no está corriendo en este entorno).
- Creé endpoint `/health` para health checks.
- Agregué modo producción con `SHOWCHANGO_ENV=production`: desactiva `/docs` y `/openapi.json`.
- Añadí meta tags de SEO/Open Graph en `base.html`.
- Mejoré el copy de la landing: flujo en 3 pasos y CTA "Crear mi set gratis".
- Creé páginas de error amigables (`error.html`) para 404 y 500.
- Escribí tests para `/health` y página 404.
- Actualicé `docs/DEPLOY.md`, `README.md` y `docs/FASES.md`; marqué F2 como completa.
- Total: 51 tests, `ruff check` limpio.

**Qué aprendí:**

- Para deploy stateless en free tier conviene tener los archivos de config listos (`fly.toml`, `render.yaml`) además del `Dockerfile`.
- Desactivar docs en producción reduce superficie de ataque y evita exponer la API internals.

**Siguiente paso:**

- F2 queda lista para deploy. F3 (inteligencia/recomendador) es opcional.
